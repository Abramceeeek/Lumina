# actionlint config. macos-26 (Xcode 26 / iOS 26 SDK) is required by the iOS
# build for Expo SDK 56 but isn't in actionlint's built-in runner-label list yet,
# so declare it here to avoid a false-positive "unknown runner label" error.
self-hosted-runner:
  labels:
    - macos-26
