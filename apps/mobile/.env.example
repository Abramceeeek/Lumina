# Copy to apps/mobile/.env and fill in from your Supabase project
# (Project Settings → API). EXPO_PUBLIC_ vars are exposed to the client — the
# anon key is safe to ship; never put the service_role key here.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
