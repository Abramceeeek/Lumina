# Server pipeline env. Copy to server/.env for local runs (gitignored).
# The service-role key bypasses RLS — keep it out of the app, repo, and logs.
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# Used from Phase 7b (synthesis):
ANTHROPIC_API_KEY=sk-ant-...
